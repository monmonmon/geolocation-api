@geolocations.map do |geolocation|
  {
    id: geolocation.id,
    latitude: geolocation.latitude,
    longitude: geolocation.longitude,
    ipaddress: geolocation.ipaddress,
    url_fqdn: geolocation.url_fqdn,
    url: geolocation_url(geolocation, format: :json)
  }
end
